{% extends 'base.html.twig' %}

{% block title %}Nouveau projet - TaskFlow
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-md-8 col-lg-6">
				<div class="card border-0 shadow-sm">
					<div class="card-header bg-primary text-white">
						<h4 class="card-title mb-0">
							<i class="fas fa-plus me-2"></i>Créer un nouveau projet
						</h4>
					</div>
					<div class="card-body p-4">
						<p class="text-muted mb-4">
							Remplissez les informations ci-dessous pour créer votre nouveau projet.
							Vous pourrez ensuite y ajouter des tâches et suivre votre progression.
						</p>

						{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}

						<div class="mb-4">
							{{ form_label(form.title, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
							<div class="input-group">
								<span class="input-group-text bg-light">
									<i class="fas fa-folder text-primary"></i>
								</span>
								{{ form_widget(form.title, {'attr': {'class': 'form-control form-control-lg'}}) }}
							</div>
							{{ form_errors(form.title) }}
						</div>

						<div class="mb-4">
							{{ form_label(form.description, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
							<div class="input-group">
								<span class="input-group-text bg-light align-items-start pt-3">
									<i class="fas fa-align-left text-primary"></i>
								</span>
								{{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': 4}}) }}
							</div>
							{{ form_errors(form.description) }}
						</div>
						
						<div class="d-grid gap-2 d-md-flex justify-content-md-between">
							<a href="{{ path('project_index') }}" class="btn btn-outline-secondary order-md-1">
								<i class="fas fa-arrow-left me-2"></i>Retour
							</a>
							<button type="submit" class="btn btn-primary btn-lg order-md-2">
								<i class="fas fa-save me-2"></i>Créer le projet
							</button>
						</div>

						{{ form_end(form) }}
					</div>
				</div>

				<div class="card border-0 bg-light mt-4">
					<div class="card-body">
						<h6 class="card-title">
							<i class="fas fa-lightbulb text-warning me-2"></i>Conseils pour un bon projet
						</h6>
						<ul class="mb-0 small">
							<li>Choisissez un titre clair et descriptif</li>
							<li>Décrivez les objectifs et le contexte du projet</li>
							<li>Vous pourrez ajouter des tâches une fois le projet créé</li>
						</ul>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}